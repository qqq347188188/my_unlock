name: Build my_unlock.dylib

on: [workflow_dispatch, push]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compile iOS arm64 dylib
        run: |
          SRC="my_unlock/my_unlock.m"
          echo "Source file: $SRC"
          xcrun --sdk iphoneos clang -dynamiclib -arch arm64 -fobjc-arc \
            -Wno-deprecated-declarations \
            -miphoneos-version-min=15.0 \
            -framework Foundation \
            -o "my_unlock.dylib" "$SRC"

      - name: Verify
        run: file my_unlock.dylib

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: my_unlock.dylib
          path: my_unlock.dylib
